from django.db import models

# Here we create the models for the relevant tables
# to test the models - 
#    1. run the command: python manage.py shell
#    2. inside the shell:
#       - import the class from this models file
#       - create a new instance of the models
#       - save the instances with the "save()" function
#    3. query for the classes using objects.all()
#  
# After step 3 - You're done. 
